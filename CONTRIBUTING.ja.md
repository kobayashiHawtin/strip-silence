# 貢献ガイド

`ableton-strip-silence` への貢献ありがとうございます。

## セキュリティ優先ルール

- **絶対に** シークレット（トークン、API キー、秘密鍵、認証情報）をコミットしないでください。
- **絶対に** ローカルの個人的なパス（例: `C:\\Users\\...`）をコミットしないでください。
- **絶対に** 個人的なオーディオプロジェクト、レンダリングメディア、または生成された出力をコミットしないでください。
- 可能な限り、ドキュメントとスクリプトで相対パスを使用してください。

## セットアップ

1. 仮想環境を作成し、有効化します。
2. プロジェクトを編集可能モードでインストールします。
3. pre-commit をインストールしてフックをセットアップします。

## PR を開く前に

ローカルで以下のチェックを実行してください:

- ユニットテスト
- 静的セキュリティスキャン
- 依存関係の脆弱性スキャン
- シークレットスキャン（pre-commit フック）

GitHub Actions ワークフローは PR に対して同じチェックを実行します。

## プルリクエスト チェックリスト

- [ ] シークレットまたはプライベート認証情報が存在しません。
- [ ] 絶対的な個人用パスが存在しません。
- [ ] テストはローカルで成功しています。
- [ ] 動作の変更は `README.md` に文書化されています。
- [ ] セキュリティに関連する変更は PR の説明で説明されています。
