# Ableton Live ⇔ Reaper 相互ワークフロー自動化スクリプト実装計画書

## 1. 開発の背景と目的

Ableton Liveで制作されたマルチトラックプロジェクトから、Reaperの高度なオーディオ編集機能（無音削除ツールなど）を利用して非破壊エディットを行い、再びAbleton Liveの正確なタイムラインへエディット済みのWav群を復元する。
手動で行うと破綻しやすい「トラックの親子（グループ）構造の維持」と「エディット後のタイミング同期」を、`.als`（XML）ファイルの解析・編集によって完全自動化する。

---

## 2. システム概要・全体ワークフロー

```text
[Phase 1]
Ableton Live (.als) ────> 【スクリプト 1】 ────> 連番リネームされたWav群 ────> Reaperへインポート
                                                   （並び順・階層が維持される）

[Phase 2]
Reaperで無音削除 ────> タイムコード付きWav書き出し ────> 【スクリプト 2】 ────> 新規・または元の .als へ自動再配置

```

---

## 3. 各フェーズの詳細仕様

### Phase 1: ALS構造解析 ＆ パラデータ・リネーマー（書き出し用）

#### 【入力（Phase 1）】

* ターゲットとなるAbleton Liveのプロジェクトファイル（`.als`）
* Liveから「すべてのトラック」でエクスポートされた生Wavファイル群が格納されたディレクトリ

#### 【処理ロジック（Phase 1）】

1. **`.als` 展開とパース:**

    * `.als` はGzip圧縮されているため、`gzip` モジュールで解凍し、XMLとしてパースする。

1. **トラック構造・階層の抽出:**

    * XML内の `<Tracks>` 要素配下を走査する。
    * `<AudioTrack>` および `<GroupTrack>` を上から順にインデックス化する。
    * グループの階層構造（親子関係）を正確に把握するため、各トラックのエレメント内にある `TrackGroupId` と `Id`（またはネスト構造）を解析し、インデックスに紐付ける。

1. **リネームルールの適用:**

    * Liveが書き出すデフォルトファイル名 `[ProjectName] - [TrackName].wav` から `[TrackName]` を前方一致などで特定する。
    * Reaperにドラッグ＆ドロップして「名前順ソート」した際に、Liveのタイムラインと「全く同じ並び順」「隣り合うグループ関係」になるよう、以下の命名規則で一括リネームする。
    * **命名規則:** `[3桁の絶対連番]_[グループ階層プレフィックス]_[TrackName].wav`
    * *(例: `001_Drums_Kick.wav`, `002_Drums_Snare.wav`, `003_Bass.wav`)*

#### 【出力（Phase 1）】

* リネーム処理されたWavファイル群

---

### Phase 2: Reaperエディット後 Wav自動配置スクリプト（戻し用）

#### 【前提となるReaperでの作業】

* ユーザーはPhase 1のWavをReaperに並べ、「Auto-trim/Split items（無音削除）」を実行。
* 書き出し時、Reaperのバウンス機能（Wildcards）を使用し、「ファイル名にタイムコード（開始秒数またはサンプル数、またはBWF埋め込み）」を含めてWavをレンダリングする。
* 推奨ファイル名ルール: `[元ファイル名]_tc_[開始サンプルの絶対数].wav`

#### 【入力（Phase 2）】

* Reaperからエクスポートされたタイムコード情報付きWav群が格納されたディレクトリ
* 復元先となるベース `.als` ファイル（Phase 1で使用した元の `.als`、または全トラックが空の `.als`）

#### 【処理ロジック（Phase 2）】

1. **Wavファイル名（またはBWFメタデータ）のパース:**

    * 各Wavファイルから、「ターゲットトラック名」と「タイムライン上の開始時間」を正規表現等で抽出する。
    * 開始サンプル数（または秒数）を、Liveのセット内テンポ（BPM）に基づいた **「Beat数（拍数）」** に逆演算する。

$$
\text{Beat数} = \frac{\text{開始サンプル数}}{\text{サンプリングレート}} \times \frac{\text{BPM}}{60}
$$

1. **ALS XMLへのクリップインサート:**

    * 対象トラックの `<MainSequencer>` ＞ `<Sample>` ＞ `<ArrangerAutomation>` ＞ `<Events>`（または `<Clips>` ）要素を特定。
    * 抽出したBeat数を元に、新しい `<AudioClip>` 要素をXML構造として生成・注入する。
    * 設定パラメータ: `<CurrentStart Value="[算出されたBeat数]" />`, `<CurrentEnd Value="..." />`
    * クリップ内のファイル参照パス（`<FileRef>`）を、該当するReaper書き出し後Wavへのパスに書き換える。

1. **再圧縮:**

    * 編集完了したXMLを再度Gzip圧縮し、拡張子 `.als` として保存する。

#### 【出力（Phase 2）】

* Reaperでの無音削除結果が、ミリ秒単位で正確な位置にタイムライン配置された `.als` ファイル

---

## 4. コーディングエージェントへの指示（Prompt）

コーディングを開始する際は、以下のプロンプトをエージェントにそのまま与えてください。

> **エージェントへの指示:**
> 上記の「詳細実装計画書」の仕様に厳密に従い、Python（3.10以上、外部依存ライブラリは最小限、XML操作は `xml.etree.ElementTree` または `lxml` を想定）で動作する2つの独立したスクリプト（または1つのコマンドラインツール）を作成してください。
> 以下の点に特に配慮してコードを堅牢に書いてください：
>
> 1. Ableton Liveの `.als` XMLは非常に巨大かつ複雑です。タグのパース時に名前空間（Namespace）のハンドリングを誤らないようにしてください。
> 2. Liveのクリップ配置時間は「秒」ではなく「Beat（拍数）」ベースです。テンポ（BPM）とサンプリングレートから正確に変換する計算ロジックを実装してください。
> 3. 各ステップでの処理成否や、ファイル名ミスマッチ時のログ出力を詳細に行ってください。
>